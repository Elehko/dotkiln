# Contributing

Dotkiln uses short-lived branches from `main`.

Branch names should follow `type/short-description`, for example:

- `feat/grouped-rollback`
- `fix/status-drift-output`
- `docs/writing-a-stack-example`

Behavior changes should include corresponding tests. Changes to update isolation or verification should receive extra review because that code protects user working trees.
